# 📊 Порівняльний аналіз: Статичний JSON vs Динамічне завантаження

## 🏗️ Архітектура

### ПОТОЧНИЙ ПІДХІД (daily.html зараз)

```
┌─────────────────┐
│  Google Sheets  │
│   (Ваші дані)   │
└────────┬────────┘
         │
         │ [GitHub Actions - кожні 3 години]
         ↓
┌─────────────────┐
│  Python Script  │
│fetch_google_... │
└────────┬────────┘
         │
         │ [Парсинг, обробка]
         ↓
┌─────────────────┐
│ daily-sales.json│
│  (Статичний)    │
└────────┬────────┘
         │
         │ [Commit & Push]
         ↓
┌─────────────────┐
│   GitHub Pages  │
└────────┬────────┘
         │
         │ [Fetch JSON]
         ↓
┌─────────────────┐
│   daily.html    │
│  (Користувач)   │
└─────────────────┘

⏱️  ЗАТРИМКА: 0-3 години
📦  РОЗМІР РЕПО: +JSON файли
🔧  СКЛАДНІСТЬ: Висока
```

---

### НОВИЙ ПІДХІД (daily-new.html)

```
┌─────────────────┐
│  Google Sheets  │
│   (Ваші дані)   │
└────────┬────────┘
         │
         │ [Publish to Web - CSV]
         │
         ↓
┌─────────────────┐
│  Google Servers │
│   (CSV Export)  │
└────────┬────────┘
         │
         │ [Fetch CSV напряму]
         ↓
┌─────────────────┐
│   daily.html    │
│  (JavaScript)   │
│  [Парсинг CSV]  │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│   Таблиця       │
│  (Користувач)   │
└─────────────────┘

⏱️  ЗАТРИМКА: 0 секунд
📦  РОЗМІР РЕПО: Без JSON
🔧  СКЛАДНІСТЬ: Низька
```

---

## 📊 Детальне порівняння

| Критерій | Статичний JSON | Динамічне завантаження |
|----------|---------------|----------------------|
| **Швидкість оновлення** | 0-3 години | Миттєво |
| **Затримка відображення** | Немає (кеш) | ~1-2 сек (завантаження) |
| **Розмір репозиторія** | Більший (+JSON) | Менший |
| **Складність налаштування** | Висока (Actions, Python) | Низька (просто URL) |
| **Залежності** | GitHub Actions, Python, pandas | Тільки JavaScript |
| **Автоматизація** | Потрібна | Не потрібна |
| **Надійність** | Залежить від Actions | Залежить від Google |
| **Доступність офлайн** | ✅ Так (кеш) | ❌ Ні |
| **Простота підтримки** | ❌ Складна | ✅ Проста |
| **Можливість відкату** | ✅ Так (Git history) | ❌ Ні |

---

## 🎯 Коли використовувати кожен підхід

### Статичний JSON - краще коли:
- ✅ Потрібен архів історичних даних
- ✅ Важлива доступність офлайн
- ✅ Є складна обробка даних (обчислення, агрегація)
- ✅ Потрібен контроль версій даних
- ✅ Критична швидкість відображення (кеш)

### Динамічне завантаження - краще коли:
- ✅ Потрібні дані в реальному часі
- ✅ Часті зміни протягом дня
- ✅ Простота важливіша за функціональність
- ✅ Немає можливості налаштувати GitHub Actions
- ✅ Дані оновлюються вручну

---

## 🔄 Що відбувається під капотом

### Статичний підхід (fetch_google_sheet.py):

```python
# 1. Завантажуємо CSV
response = requests.get(SHEET_URL)

# 2. Парсимо pandas
df = pd.read_csv(...)

# 3. Очищаємо дані
def clean_number(val):
    # Складна логіка обробки
    ...

# 4. Конвертуємо в JSON
data = df.to_dict('records')
with open('daily-sales.json', 'w') as f:
    json.dump(data, f)

# 5. Git commit & push (автоматично)
```

### Динамічний підхід (JavaScript):

```javascript
// 1. Завантажуємо CSV
const response = await fetch(SHEET_URL);
const text = await response.text();

// 2. Парсимо вручну
const rows = parseCSV(text);

// 3. Будуємо таблицю
rows.forEach(row => {
    // Створюємо <tr>, <td>
    // Застосовуємо стилі
});

// Готово!
```

---

## 💾 Розмір даних

### Приклад для 10 продавців + 3 службові рядки:

**JSON (daily-sales.json):**
```json
[
  {
    "ПК": "Алтухов Олександр Сергійович",
    "ТО": "123456",
    "Послуги": "5000",
    ...
  },
  ...
]
```
Розмір: **~3-5 KB** (форматований) або **~2 KB** (мініфікований)

**CSV (Google export):**
```csv
ПК,ТО,Послуги,АСС,...
Алтухов Олександр Сергійович,123456,5000,2000,...
...
```
Розмір: **~1.5 KB**

📉 CSV компактніший, але JSON зручніший для обробки

---

## ⚡ Продуктивність

### Завантаження сторінки:

| Підхід | Розмір завантаження | Час парсингу | Загальний час |
|--------|-------------------|-------------|--------------|
| JSON | 3 KB | ~5 ms | ~100-200 ms |
| CSV динамічний | 1.5 KB | ~10 ms | ~1000-2000 ms* |

\* *Включає затримку мережі Google Sheets*

### Висновок:
- JSON швидший для відображення
- CSV актуальніший за даними

---

## 🛠️ Підтримка та оновлення

### Статичний JSON:

**Оновлення даних:**
1. Змінити Google Sheets
2. Дочекатись GitHub Actions (0-3 год) АБО
3. Запустити Actions вручну
4. Дочекатись GitHub Pages rebuild (~2 хв)

**Зміна структури:**
1. Оновити Google Sheets
2. Перевірити `fetch_google_sheet.py`
3. Можливо, змінити код парсингу
4. Тестувати локально
5. Commit & Push
6. Перевірити Actions

**Складність:** ⭐⭐⭐⭐

---

### Динамічний CSV:

**Оновлення даних:**
1. Змінити Google Sheets
2. Оновити сторінку (F5)

**Зміна структури:**
1. Оновити Google Sheets
2. Якщо нові колонки - оновлюється автоматично
3. Якщо потрібні нові кольори - змінити CSS класи

**Складність:** ⭐

---

## 🔐 Безпека

### Статичний JSON:
- ✅ Контроль доступу через GitHub
- ✅ Історія змін
- ✅ Можливість відкату
- ❌ Публічний JSON (якщо Pages публічні)

### Динамічний CSV:
- ⚠️ Публічна таблиця Google Sheets
- ❌ Немає історії змін на сервері
- ❌ Неможливий відкат
- ✅ Можна обмежити домени (Google Sheets settings)

---

## 🎨 Кастомізація

### Обидва підходи підтримують:
- ✅ Автоматичне визначення колонок
- ✅ Кольорові групи (ТО, Послуги, АСС, Чеки)
- ✅ Спеціальні рядки (час, відсотки, підсумки)
- ✅ Адаптивний дизайн
- ✅ Свайп-навігація

---

## 🏆 Рекомендації

### Для Daily Sales:
**→ Динамічне завантаження** ✅

**Чому:**
- Дані змінюються протягом дня
- Потрібна актуальність
- Не потрібна історія
- Простіше в підтримці

### Для Plan-Fact:
**→ Вже використовує динамічне** ✅

### Для Evening Report (index.html):
**→ Може залишитись статичним**

**Чому:**
- Звіт формується 1 раз на день
- Потрібна історія для аналізу
- Складні обчислення та метрики
- Використовується для довгострокового аналізу

---

## 🚀 План міграції

### Фаза 1: Daily Sales (ЗАРАЗ) ✅
```
daily.html: JSON → CSV
```

### Фаза 2: Тестування (1-2 дні)
- Перевірити стабільність
- Порівняти швидкість
- Збір фідбеку

### Фаза 3: Опціонально
```
index.html: JSON → CSV (якщо потрібно)
```

---

## 📈 Статистика використання

### GitHub Actions (поточний підхід):

```yaml
Runs: ~8 на день (кожні 3 год)
Duration: ~30-60 сек на run
Total compute: ~5-8 хвилин на день
```

### Динамічне завантаження:

```
API calls: ~100-500 на день (залежить від відвідувачів)
Google Sheets quota: Unlimited для published sheets
Total compute: 0 (client-side)
```

**Економія:** ~100% GitHub Actions usage для daily sales

---

## ✅ Висновок

### Daily Sales → Динамічне завантаження
- ✅ **Рекомендовано**
- ✅ Простіше
- ✅ Швидше
- ✅ Актуальніше

### Index.html (Evening Report) → Залишити статичним
- ✅ **Рекомендовано**
- ✅ Історія даних
- ✅ Складна обробка
- ✅ Стабільність

---

**Обидва підходи мають своє призначення!** 🎯
