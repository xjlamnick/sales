# 🔄 Повна інструкція оновлення проекту

## 📦 Список оновлених файлів

### 1. `daily-new.html` → `daily.html`
Новий файл з динамічним завантаженням

### 2. `fetch_google_sheet.py` (ОНОВЛЕНО)
Видалено генерацію daily-sales.json

### 3. `.gitignore` (ОНОВЛЕНО)
Додано daily-sales.json та daily-old.html

---

## 🚀 Покрокова інструкція

### Крок 1: Підготовка

Переконайтесь що у вас є:
- ✅ Доступ до вашого GitHub репозиторію
- ✅ Git встановлений локально
- ✅ Google Sheets з daily sales опубліковано (CSV)

---

### Крок 2: Оновіть URL в daily-new.html

Відкрийте `daily-new.html` та знайдіть рядок 107:

```javascript
const SHEET_URL = "https://docs.google.com/spreadsheets/d/e/2PACX-...";
```

**ЗАМІНІТЬ** на URL вашої таблиці з daily sales.

**Як отримати URL:**
1. Відкрийте вашу Google Таблицю з щоденними продажами
2. File → Share → Publish to web
3. Оберіть потрібний аркуш (Sheet)
4. Формат: **Comma-separated values (.csv)**
5. Натисніть **Publish**
6. Скопіюйте URL

---

### Крок 3: Замініть файли в проекті

```bash
# Перейдіть в директорію проекту
cd /path/to/your/sales-project

# Створіть бекап старих файлів (на всякий випадок)
cp daily.html daily-old.html
cp fetch_google_sheet.py fetch_google_sheet-old.py
cp .gitignore .gitignore-old

# Замініть файли новими (скопіюйте з архіву)
# Або вручну скопіюйте вміст файлів
```

---

### Крок 4: Перейменуйте daily-new.html

```bash
# Видаліть старий daily.html
rm daily.html

# Перейменуйте новий файл
mv daily-new.html daily.html
```

---

### Крок 5: Видаліть непотрібний файл (опціонально)

```bash
# daily-sales.json більше не потрібен
# Але не видаляйте його одразу - спершу протестуйте!
# git rm daily-sales.json
```

---

### Крок 6: Commit і Push

```bash
# Додайте всі зміни
git add daily.html fetch_google_sheet.py .gitignore

# Зробіть commit
git commit -m "Перехід daily sales на динамічне завантаження

- daily.html тепер завантажує дані напряму з Google Sheets
- fetch_google_sheet.py більше не генерує daily-sales.json
- Оновлено .gitignore"

# Завантажте на GitHub
git push
```

---

### Крок 7: Перевірка

1. Дочекайтесь оновлення GitHub Pages (~1-2 хвилини)
2. Відкрийте ваш сайт
3. Перейдіть на сторінку "Щоденні звіти"
4. Має з'явитись:
   - "Завантаження даних з Google Sheets..."
   - Потім таблиця з даними
   - Кольорові колонки (ТО, Послуги, АСС, Чеки)

---

## 🧪 Тестування

### Тест 1: Завантаження даних

1. Відкрийте daily.html
2. Відкрийте консоль браузера (F12)
3. Перевірте що немає помилок
4. Дані мають відображатись

### Тест 2: Оновлення в реальному часі

1. Змініть щось в Google Sheets
2. Збережіть (Ctrl+S)
3. Оновіть сторінку daily.html (F5)
4. Зміни мають відобразитись миттєво

### Тест 3: GitHub Actions

```bash
# Локально запустіть оновлений скрипт
python fetch_google_sheet.py
```

Має вивестись:
```
Завантаження основних даних для вечірнього звіту...
✓ sales-data.json успішно оновлений
ℹ️  daily-sales.json більше не генерується - daily.html завантажує дані напряму з Google Sheets

📊 Оброблено продавців: 10
💰 Загальний товарообіг: 2,510,143.80 грн
```

---

## 📊 Що відбувається після оновлення

### РАНІШЕ:

```
GitHub Actions (кожні 3 год)
    ↓
fetch_google_sheet.py
    ↓ генерує
daily-sales.json ← використовує daily.html
    ↓
sales-data.json ← використовує index.html
```

### ТЕПЕР:

```
GitHub Actions (кожні 3 год)
    ↓
fetch_google_sheet.py
    ↓ генерує ТІЛЬКИ
sales-data.json ← використовує index.html

daily.html ← завантажує напряму з Google Sheets
```

---

## ✅ Переваги нової структури

1. **Простіше** - менше файлів для підтримки
2. **Швидше** - daily sales оновлюються миттєво
3. **Надійніше** - менше точок відмови
4. **Зрозуміліше** - кожна сторінка має свій спосіб отримання даних

---

## 🗂️ Структура після оновлення

```
sales-project/
├── .github/
│   └── workflows/
│       └── auto-update.yml      (без змін - оновлює sales-data.json)
│
├── daily.html                    (ОНОВЛЕНО - динамічне завантаження)
├── index.html                    (без змін - використовує sales-data.json)
├── plan-fact.html                (без змін - динамічне завантаження)
├── plan-fact-2.html              (без змін)
│
├── fetch_google_sheet.py         (ОНОВЛЕНО - без daily-sales.json)
├── .gitignore                    (ОНОВЛЕНО - ігнорує daily-sales.json)
│
├── sales-data.json               (генерується Actions - для index.html)
└── daily-sales.json              (ВИДАЛИТИ після тестування)
```

---

## 🔧 Налаштування GitHub Actions

Ваш workflow `.github/workflows/auto-update.yml` залишається БЕЗ ЗМІН.

Він продовжує:
- ✅ Запускатись кожні 3 години
- ✅ Генерувати `sales-data.json` для вечірнього звіту
- ✅ Commit і push змін

Просто тепер він НЕ генерує `daily-sales.json` - але це нормально!

---

## ⚠️ Важливі моменти

### 1. Не видаляйте sales-data.json!

Цей файл потрібен для `index.html` (вечірній звіт). Видаляємо ТІЛЬКИ `daily-sales.json`.

### 2. Протестуйте перед видаленням

Не видаляйте `daily-sales.json` одразу. Спершу:
1. Перевірте що `daily.html` працює
2. Поспостерігайте 1-2 дні
3. Потім видаліть і зробіть commit

### 3. Збережіть бекапи

```bash
# На всякий випадок:
cp daily-old.html ~/backups/
cp fetch_google_sheet-old.py ~/backups/
```

---

## 🆘 Що робити якщо щось зламалось

### Проблема: daily.html не завантажує дані

**Рішення:**
1. Перевірте консоль браузера (F12)
2. Перевірте URL Google Sheets в коді
3. Перевірте що таблиця опублікована

### Проблема: GitHub Actions падає з помилкою

**Рішення:**
```bash
# Локально запустіть скрипт
python fetch_google_sheet.py

# Подивіться на помилку
# Якщо все ОК локально - перезапустіть Actions
```

### Проблема: Хочу повернутись до старої версії

**Рішення:**
```bash
# Поверніть старі файли
mv daily-old.html daily.html
mv fetch_google_sheet-old.py fetch_google_sheet.py

git add daily.html fetch_google_sheet.py
git commit -m "Rollback to old version"
git push
```

---

## 📞 Чек-лист перед завершенням

- [ ] URL Google Sheets вставлено в daily-new.html
- [ ] Файли замінено в проекті
- [ ] daily-new.html перейменовано на daily.html
- [ ] Зміни закомічено та запушено
- [ ] Сайт оновився на GitHub Pages
- [ ] daily.html відкривається без помилок
- [ ] Дані відображаються правильно
- [ ] Тест оновлення: змінили Sheets → оновили сторінку → бачимо зміни
- [ ] fetch_google_sheet.py працює локально
- [ ] GitHub Actions відпрацював успішно
- [ ] sales-data.json генерується
- [ ] index.html працює (вечірній звіт)

---

## 🎉 Готово!

Тепер ваш проект має:
- ✅ Daily sales в реальному часі
- ✅ Вечірній звіт через Actions
- ✅ План-факт динамічно
- ✅ Простішу структуру
- ✅ Менше файлів для підтримки

**Успіхів!** 🚀
