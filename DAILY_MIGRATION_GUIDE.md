# 📊 Інструкція: Перехід Daily Sales на динамічне завантаження

## ✨ Що змінюється?

### БУЛО (статичний JSON):
```
Google Sheets → GitHub Actions → Python скрипт → JSON файл → Сайт
```
- Потрібна автоматизація через GitHub Actions
- Дані оновлюються кожні 3 години або вручну
- Файл daily-sales.json зберігається в репозиторії

### СТАЄ (динамічне завантаження):
```
Google Sheets → Сайт (напряму)
```
- Дані завантажуються в реальному часі
- Оновлення при кожному відкритті сторінки
- Не потрібен проміжний JSON файл

---

## 🔧 Що потрібно зробити

### Крок 1: Опублікуйте таблицю з Daily Sales

1. Відкрийте вашу Google Таблицю з щоденними продажами
2. **File** → **Share** → **Publish to web**
3. Оберіть:
   - **Tab:** потрібний аркуш (Sheet) з daily sales
   - **Format:** CSV
4. Натисніть **Publish**
5. **СКОПІЮЙТЕ URL** який виглядає так:
   ```
   https://docs.google.com/spreadsheets/d/e/2PACX-xxxxx/pub?output=csv
   ```

### Крок 2: Оновіть daily.html

У файлі `daily-new.html` знайдіть рядок 107:

```javascript
const SHEET_URL = "https://docs.google.com/spreadsheets/d/e/2PACX-1vQOxz...";
```

**ЗАМІНІТЬ** на ваш URL з кроку 1!

### Крок 3: Перейменуйте файли

```bash
# Створіть бекап старого файлу
mv daily.html daily-old.html

# Перейменуйте новий файл
mv daily-new.html daily.html
```

### Крок 4: Завантажте на GitHub

```bash
git add daily.html
git commit -m "Перехід daily sales на динамічне завантаження"
git push
```

Готово! ✅

---

## 📋 Формат таблиці Google Sheets

Ваша таблиця має містити:

### Рядок 1 (заголовки):
```
ПК | ТО | Послуги | АСС | Чеки | Дохід | ЗК | TOTAL UAH | СрЧек
```

### Рядки 2+: Дані продавців
```
Алтухов Олександр Сергійович | 123456 | 5000 | 2000 | 15 | ... 
Бондаренко Володимир... | 234567 | 6000 | 3000 | 20 | ...
```

### Спеціальні рядки:

1. **Рядок з часом** (автоматично виділяється):
   ```
   06:59:13 | 0 | 0 | 0 | ...
   ```
   Має містити час у форматі `HH:MM:SS` в першій колонці

2. **Рядок з відсотками**:
   ```
   0 | 0% | 0,00% | 0,00% | ...
   ```

3. **Рядок "Залишок"**:
   ```
   Залишок | 1043650 | 34440 | ...
   ```

---

## 🎨 Автоматичні кольори стовпців

Скрипт автоматично застосовує кольори:

| Назва стовпця | Колір | Клас CSS |
|--------------|-------|----------|
| **ТО** | Синій (#0071e3) | `.col-to` |
| **Послуги** | Фіолетовий (#af52de) | `.col-poslugi` |
| **АСС** або **Аксесуари** | Помаранчевий (#ff9500) | `.col-acc` |
| **Чеки** | Зелений (#34c759) | `.col-cheky` |

Назви стовпців можуть бути:
- `ТО`, `Послуги`, `АСС`, `Чеки` ✅
- `ПОСЛУГ`, `АКСЕСУАРИ`, `ЧЕК` ✅
- Регістр не важливий

---

## 🔍 Особливості нової версії

### ✅ Переваги:
1. **Реальний час** - дані завжди актуальні
2. **Простота** - змінили в Google Sheets → оновили сторінку → побачили зміни
3. **Немає затримок** - не потрібно чекати GitHub Actions
4. **Менше коду** - не потрібен Python скрипт для daily sales

### ⚙️ Як працює:
1. Користувач відкриває `daily.html`
2. JavaScript робить запит до Google Sheets (CSV)
3. Парсить CSV дані
4. Будує таблицю динамічно
5. Застосовує стилі та кольори

### 🔄 Оновлення даних:
- **Автоматично** при кожному відкритті сторінки
- **Кешування браку** через параметр `?t=` з timestamp
- Дані завжди свіжі!

---

## 🚨 Можливі проблеми та рішення

### Проблема: "Немає даних"

**Причина:** Таблиця не опублікована або URL неправильний

**Рішення:**
1. Перевірте що таблиця опублікована (Publish to web)
2. Перевірте URL - має містити `/pub?output=csv`
3. Відкрийте URL в браузері - має завантажитись CSV файл

---

### Проблема: "Помилка завантаження"

**Причина:** CORS або проблема з мережею

**Рішення:**
1. Перевірте інтернет з'єднання
2. Переконайтесь що таблиця публічна (anyone with link)
3. Спробуйте оновити сторінку (F5)

---

### Проблема: Дані відображаються неправильно

**Причина:** Формат таблиці не відповідає очікуваному

**Рішення:**
1. Перевірте що перший рядок - заголовки
2. Перевірте що всі рядки мають однакову кількість колонок
3. Видаліть порожні рядки в таблиці

---

## 📊 Порівняння з іншими сторінками

### План-Факт (`plan-fact.html`):
- ✅ Динамічне завантаження з Google Sheets
- ✅ Парсинг CSV
- ✅ Форматування чисел
- 🎯 **ТЕПЕР daily.html працює так само!**

### Вечірній звіт (`index.html`):
- ❌ Використовує статичний `sales-data.json`
- ❌ Потребує GitHub Actions для оновлення
- 🔄 Можна також перевести на динамічне завантаження

---

## 🎯 Наступні кроки (опціонально)

### 1. Видалити непотрібні файли

Після переходу на динамічне завантаження можна видалити:

```bash
# Файли які більше не потрібні для daily sales:
rm daily-sales.json         # Старий JSON файл
rm daily-old.html           # Бекап старого файлу
```

**Увага:** Не видаляйте `fetch_google_sheet.py` якщо він використовується для інших сторінок!

---

### 2. Оновити GitHub Actions

У файлі `.github/workflows/auto-update.yml` можна виключити daily sales:

```yaml
# Якщо daily sales тепер динамічний, можна не генерувати JSON
# Залиште тільки для sales-data.json (вечірній звіт)
```

---

### 3. Додати автооновлення

Якщо хочете щоб таблиця автоматично оновлювалась (наприклад, кожні 5 хвилин):

```javascript
// Додайте в кінець скрипту в daily.html:

// Автооновлення кожні 5 хвилин
setInterval(() => {
    console.log("Автооновлення даних...");
    location.reload();
}, 5 * 60 * 1000); // 5 хвилин в мілісекундах
```

---

## ✅ Контрольний список

Перед запуском:

- [ ] Google Таблиця опублікована (Publish to web → CSV)
- [ ] URL скопійовано та вставлено в `daily-new.html`
- [ ] Файл перейменовано на `daily.html`
- [ ] Зміни завантажено на GitHub (`git push`)
- [ ] Сторінка відкривається без помилок
- [ ] Дані відображаються правильно
- [ ] Кольори стовпців застосовані
- [ ] Рядок з часом виділяється

---

## 🎉 Готово!

Тепер вашDaily Sales працює так само як Plan-Fact:
- ✅ Динамічне завантаження
- ✅ Дані в реальному часі
- ✅ Без GitHub Actions
- ✅ Простіше та швидше!

Змінили в Google Sheets → Оновили сторінку → Бачимо нові дані! 🚀
