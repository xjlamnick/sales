# 🔄 Оновлення проекту - видалено daily-sales.json

## 📦 Нові/оновлені файли

### 1. `fetch_google_sheet.py` ⚙️
**Статус:** ОНОВЛЕНО

**Що змінилось:**
- ❌ Видалено генерацію `daily-sales.json`
- ❌ Видалено змінну `DAILY_SALES_URL`
- ✅ Залишено тільки генерацію `sales-data.json` (для index.html)
- ✅ Додано інформативні повідомлення

**Використання:**
```bash
python fetch_google_sheet.py
```

**Вивід:**
```
Завантаження основних даних для вечірнього звіту...
✓ sales-data.json успішно оновлений
ℹ️  daily-sales.json більше не генерується - daily.html завантажує дані напряму з Google Sheets

📊 Оброблено продавців: 10
💰 Загальний товарообіг: 2,510,143.80 грн
```

---

### 2. `.gitignore` 🚫
**Статус:** ОНОВЛЕНО

**Що додано:**
```gitignore
# Бекапи старих файлів
daily-old.html
*-old.*

# daily-sales.json більше не використовується
daily-sales.json
```

**Чому:**
- `daily-sales.json` тепер не потрібен (daily.html завантажує напряму)
- Бекапи не мають потрапляти в репозиторій

---

### 3. `FULL_UPDATE_GUIDE.md` 📚
**Статус:** НОВИЙ

Повна покрокова інструкція:
- Як замінити всі файли
- Тестування
- Вирішення проблем
- Чек-лист

---

## 🎯 Швидкий старт

### Варіант А: Тільки оновити скрипт (рекомендовано після встановлення daily.html)

```bash
# 1. Замініть fetch_google_sheet.py новою версією
cp fetch_google_sheet.py /path/to/your/project/

# 2. Замініть .gitignore
cp .gitignore /path/to/your/project/

# 3. Commit
git add fetch_google_sheet.py .gitignore
git commit -m "Оновлено fetch_google_sheet.py - видалено daily-sales.json"
git push

# 4. Видаліть старий файл (опціонально, після тестування)
git rm daily-sales.json
git commit -m "Видалено непотрібний daily-sales.json"
git push
```

---

### Варіант Б: Повне оновлення (якщо ще не встановили daily.html)

Дивіться `FULL_UPDATE_GUIDE.md`

---

## 📊 Що тепер генерується

| Файл | Генерується | Використовується |
|------|------------|-----------------|
| `sales-data.json` | ✅ GitHub Actions | `index.html` (вечірній звіт) |
| `daily-sales.json` | ❌ ВИДАЛЕНО | ~~daily.html~~ |

**Замість `daily-sales.json`:**
- `daily.html` завантажує дані напряму з Google Sheets
- Оновлення в реальному часі
- Немає проміжних файлів

---

## ⚠️ Важливо!

### ДО заміни файлів:

Переконайтесь що:
1. ✅ `daily.html` вже оновлено до нової версії (dynamic loading)
2. ✅ `daily.html` протестовано та працює
3. ✅ У вас є бекапи старих файлів

### ПІСЛЯ заміни файлів:

1. Протестуйте `fetch_google_sheet.py` локально
2. Перевірте GitHub Actions
3. Переконайтесь що `index.html` працює
4. Тільки тоді видаляйте `daily-sales.json`

---

## 🧪 Тестування

### Тест 1: Локально
```bash
python fetch_google_sheet.py
```

Має з'явитись повідомлення про `daily-sales.json` (що він більше не генерується).

### Тест 2: GitHub Actions

1. Запустіть workflow вручну
2. Перевірте логи
3. Має бути SUCCESS
4. Має згенеруватись тільки `sales-data.json`

### Тест 3: Сайт

1. `index.html` - працює (використовує sales-data.json)
2. `daily.html` - працює (використовує Google Sheets)
3. `plan-fact.html` - працює (використовує Google Sheets)

---

## 📁 Структура проекту після оновлення

```
sales-project/
├── .github/workflows/
│   └── auto-update.yml           (без змін)
├── daily.html                    (dynamic - Google Sheets)
├── index.html                    (static - sales-data.json)
├── plan-fact.html                (dynamic - Google Sheets)
├── fetch_google_sheet.py         (ОНОВЛЕНО - тільки sales-data.json)
├── .gitignore                    (ОНОВЛЕНО)
└── sales-data.json               (генерується)
```

**Видалено:**
- ~~daily-sales.json~~

---

## 🔄 Workflow після оновлення

```
GitHub Actions (кожні 3 год)
    ↓
fetch_google_sheet.py
    ↓
Генерує ТІЛЬКИ sales-data.json
    ↓
index.html (вечірній звіт)


daily.html → Google Sheets (direct)
plan-fact.html → Google Sheets (direct)
```

---

## ✅ Переваги

| Було | Стало |
|------|-------|
| 2 JSON файли | 1 JSON файл |
| GitHub Actions генерує обидва | GitHub Actions генерує тільки один |
| daily.html чекає 0-3 год | daily.html оновлюється миттєво |
| Більше коду | Менше коду |

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Прочитайте `FULL_UPDATE_GUIDE.md`
2. Перевірте логи GitHub Actions
3. Протестуйте локально
4. Поверніть бекапи якщо потрібно

---

**Готово до використання!** ✅
